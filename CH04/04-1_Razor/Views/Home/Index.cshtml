
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div> 

        @{

            int m = 10;
            string s = "<h1>Hello</h1>";
            string[] data = { "a","b","c"};


         }


        <ul>
            <li>m=@m</li>
            <li>s=@s</li>
            <li>s=@Html.Raw(s)</li>
        </ul>

        <hr>

        
            @{ 
                for (int i = 0; i < data.Length; i++)
                {
                    
                    @data[i];
                    @:<br />
                    //說明上面這一行 如果有文字要在C#區顯示的話 前面要加 @:
                }

            }

        <hr>
        <ul>
            @foreach (var item in data)
            {
                <li>項目: @item</li>
            }

            @{

                foreach (var item in data)
                {
                    <li>項目: @item</li>
                }

            }
        </ul>


        @*if*@
        @if (m == 10)
        {
            @:m == 10 <br />
            @:@@ <br />
            @:john@uuu.com<br />
        }

        @{

            if (m == 10)
            {
                <text>
                    m == 10 <br />
                    m == 10 <br />
                    m == 10 <br />
                    m == 10 <br />
                    @@ <br />
                    john@uuu.com<br />
                </text>
            }

        }

        <hr>

        <video src="~/Content/movie.mp4" controls></video>
        <video src="~/Home/GetVideo" controls></video>
        <video src="@Url.Action("GetVideo", "Home")" controls></video>
    </div>
</body>
</html>
